import java.util.HashMap;  //HashMap 가져오기

public class maptp {
    public static void main(String[] args) {
        //맵(map) - 대응관계를 쉽게 표현할 수 있게 해주는 자료형
       //         - 순차적으로 해당 요소 값을 구하지 않고 key를 통해 value를 얻는다
       //         - list와 마찬가지로 인터페이스다
       //         - 맵 자료형에는 HashMap, LinkedHashMap, TreeMap등이 있다
       HashMap<String, String> map = new HashMap<String, String>();
       map.put("people", "사람");  //입력
       map.put("baseball", "야구");

       System.out.println(map.get("people")); //key에 해당되는 값을 얻을 수 있다
       System.out.println(map.containsKey("people")); //해당 key가 있는지 조사
       System.out.println(map.remove("people")); //해당 key와 value를 삭제
       System.out.println(map.size()); //map의 갯수 리턴
    }
}
